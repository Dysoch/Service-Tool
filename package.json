{
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "convert-docs": "node convert-docs.js"
    },
    "devDependencies": {
        "concurrently": "^9.0.0",
        "mammoth": "^1.10.0"
    }
}